import db
import users
import categories
import user_categories
import timetable
import os
import server

# if __name__ == "__main__":
#     print(os.getenv('variable'))
    # db.check_init_db()
    # users.add_user_to_db(1)
    # categories.add_category('Food, Cases, Chill, MolBio')
    # user_categories.add_user_categories(1, 'Food, Cases, Chill, MolBio')
    # user_categories.get_user_categories(1)
    # timetable.add_time_note(1, 'Chill 11:30-14:00')
    # timetable.add_time_note(1, 'Food 14:00-14:30')
    # timetable.add_time_note(1, 'Chill 14:30-17:00')
    # timetable.add_time_note(1, 'MolBio 17:00-19:00')
    # timetable.add_time_note(1, 'Chill 19:00-23:30')
    # timetable.add_time_note(1, 'Chill 19:00-23:30')
    # timetable.add_time_note(1, 'jj 14:00-19:00')